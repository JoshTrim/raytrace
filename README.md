# raytrace

## compilation command
gcc -Wall -Wextra -lm -g -o raytracing raytracing.c `sdl2-config --cflags --libs`

## todo
- [ ] make rays end at screen border (i.e replace/modify length param)
- [ ] make rays collide with other objects
